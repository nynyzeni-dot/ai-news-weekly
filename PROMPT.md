# AIニュース週報 作成手順

毎週月曜の朝、クラウドで自動実行される手順書。上から順に最後まで実行すること。

## 1. 日付と対象期間を確定

★この手順書の日付はすべて日本時間（JST）で扱う。UTCを使わないこと（毎週月曜7時JSTの実行はUTCでは日曜になり、対象期間が1日ズレるため）。

`TZ=Asia/Tokyo date +%Y-%m-%d` で今日の日付を取得する。対象期間は **今日から遡って7日間**。この期間外のニュースは載せない。

出力ファイル名は `weeks/<今日の日付>.html` になる。

## 2. 過去回を読んで重複を避ける

`weeks/` の中のHTMLファイル一覧を見て、**直近3回分を読む**。そこで既に取り上げたニュースは今回は載せない。

ファイル数から今回が「第何回」かを決める（既存が3ファイルなら今回は第4回）。

## 3. ニュースを集める

### 読む人
美容室経営コンサルタント。エンジニアではないが、Claude Code / Notion / LINE Bot / 動画編集ツールを日常的に自分で触る実務家。自分でアプリも作る（Next.js / FastAPI / VPS運用）。

**制作の実際（採用判定に使う）**
- リールは**PC上で完結**して作る（ffmpegでの組み立て、AI画像生成、自作スクリプト）。**スマホの動画編集アプリの中で編集する工程が無い**
- 主力の「漫画リール」は**声が入ってへん**。文字を読ませる作りで、**BGMも動画に焼き込まん**（Instagram側の音楽ライブラリで後から付ける）。音声・BGM・声の編集に関する話は、下の「採用の条件」の追加ルールに従う
- ★**実写の自撮り動画はこれから始める段階**（撮影機材＝DJI Osmo Pocket 4P Vlogコンボを2026-09-02に発注済み・2026-09-04着予定。**機材が届いてもそれだけでは撮影開始にならない＝実際に撮り始めたとこの手順書に明記されるまでは、下の音声・BGM系の解禁条件は満たさない**）。始まれば声が入るので、音声まわりの話はその時点で価値が出る
- **既に日常的に使っとる**：Claude Code / Claude in Chrome / Notion / LINE Bot / VPS / ffmpeg。画像生成は下の内訳を見ること。**「正式に一般提供されました」系のニュースは、読み手が既に使っとる道具なら価値が無い**
- ★**画像生成の道具は用途ごとに固定されている**（2026-09-01確定）：note本文の写真＝Codex（`gpt-image-2`）／noteの見出し画像＝自作Pythonスクリプト`eyecatch.py`（AI生成ではなく文字を描画するだけ）／ゴリラ漫画＝ChatGPT（Claude in Chrome経由）／それ以外＝higgsfield（`soul_2`）。**Midjourney・Stable Diffusion・Adobe Firefly・Ideogram・DALL-E単体など、この4つに入らない画像生成ツールの新機能は、読み手がその場面で使う予定が無いので、それだけでは採用理由にならない**
- **使ってへん**：Google Chat、スマホの動画編集アプリ、Photoshop（2026-09-01に本人へ確認済み＝契約してへん）
- 読み手自身の主な実務＝議事録・アンケート集計と分析・顧客向け資料とLP制作・Notion運用・LINE Bot運用・VPS運用・コラム執筆。ここに効く道具も採用対象

**クライアントの実際（観点Bのツール判断に関わる）**
- クライアントは美容室（複数店舗のグループを含む）
- ★**クライアントはInstagramリールをほとんど出してへん**。リールの企画論・撮り方の話は現場で使われん
- クライアントに刺さるのは：**物販・予約・顧客フォロー・組織や人材や採用**に関わるツール・仕組みの話

### 集める観点は2つだけ

**(A) 自分のキャッチアップ**
新モデル・新機能・仕様変更のうち「知らんかったけど今すぐ使える」もの。特に Claude / ChatGPT / Gemini の、実際に触れる機能の更新。

**(B) 業務に使えるツール**
議事録・文字起こし・データ分析・動画編集・SNS発信・資料作成・画像生成 のいずれかに直接効く新ツールや新機能。「話題になってる」だけでなく「今日から使える」ものを優先。

### 集めないもの
株価・資金調達・買収・人事・規制や訴訟・研究論文・ベンチマーク比較・「AIが人類を〜」系の論説。

### 採用の条件
- ★★**採る前に、「この読み手のどの作業に効くか」または「クライアントのどの場面で使えるか」を、上の『読む人』の実際に照らして具体的に1つ名指しで書けるか確かめる。書けんかったら、どれだけ新しくても具体的でも落とす。** 記事の内容が正しい・詳しいだけでは採用理由にならない
- ★**読み手が使ってへん／持ってへん道具（Google Chat／スマホの動画編集アプリ／Photoshop 等）の中でしか使えん機能は、どれだけ新しくても落とす。PC上の同等作業に置き換えたら効く、という理屈で採るな。** クライアントが使う場面を挙げるのも同様に禁止（例：「Photoshopで店販POPが作れる」は、読み手がPhotoshopを持ってへんので不採用）
- ★**音声・BGM・声の編集に関する話（EQ・ダッキング・ミキシング等の数値設定を含む）は、実写の自撮り動画の撮影が始まるまでは採らない。** 現状の主力（漫画リール）は無音・BGM後付けのため、編集する対象がそもそも存在しない。実写が始まったら（上の『制作の実際』を参照）解禁する。★**解禁は『制作の実際』の記述が更新されたときだけ。撮影が始まったかを自分で推測して解禁するな。**

### 調べ方
WebSearch を角度を変えて **最低5回以上** 回す（新モデルのリリース、主要AIツールのアップデート、動画/画像生成の新機能、業務効率化ツール など）。**日本語と英語の両方**で検索する。

### 件数
8〜12件。重要度の低いものは自分で落とす。該当が少なければ8件未満でよい。
**絶対に水増ししない。架空のニュースを作らない。**

## 4. 出典を実地確認する（最重要）

候補それぞれについて **WebFetch で実際にページを開き**、次を確認する：

- 中身が本当にその内容か
- 日付が対象期間内か

**開けないもの・内容が違うもの・日付が合わないものは載せない。** 検索結果のタイトルだけで書かない。

この工程を省くとリンク切れや誤情報が混ざり、週報の価値がゼロになる。第1回では実際にここで1件（リンク切れ）を落としている。

## 5. HTMLを作る

まず `index.html` を読み、**デザイン・CSS・カード構造をそのまま流用する**。見た目は毎週同じに保つこと。中身だけ差し替える。

### 各記事に必要な要素
- 重要度バッジ（高／中）
- 見出し（30字以内・日本語）
- **何があったか**：2〜3行。専門用語には（＝〜のこと）と短い説明を付ける
- **何ができるようになるか**：1〜2行。この読み手にとって何が変わるか。**記事の要約ではなく「読み手の◯◯という作業が、こう変わる」の形で書く**（採用の条件で名指しした効き先をそのまま文章にする）
- 出典リンク（`target="_blank" rel="noopener"`）と日付

### ページ全体
- 冒頭に対象期間と「第N回」
- 「今週の一言まとめ」を3行以内。文体は関西弁まじりの話し言葉で、既存ページのトーンに合わせる
- 外部CDN・外部フォント・外部画像は一切使わない。CSSは全部インライン
- スマホで横スクロールが起きないこと

### 出力する2ファイル
1. `weeks/YYYY-MM-DD.html` … 今日の日付。その週の保存版
2. `index.html` … 最新週の内容に更新。**末尾に `weeks/` 配下の過去回へのリンク一覧**を新しい順で付ける

## 6. latest.json を書く（LINE通知用・必須）

VPS側のスクリプトがこのファイルを読んでLINEに投げる。**掲載0件の週も必ず書く**。ルート直下 `latest.json` に以下の形式で書く：

```json
{
  "date": "2026-08-24",
  "count": 5,
  "url": "https://nynyzeni-dot.github.io/ai-news-weekly/weeks/2026-08-24.html",
  "items": [
    {"title": "見出し", "why": "なんで拾ったか一行", "url": "出典URL"}
  ]
}
```

- `date` は**その週の週報の日付**（ファイル名 `weeks/YYYY-MM-DD.html` と同じ日付）
- `url` はその週のHTMLのGitHub Pages URL。0件の週は省略するかnullにする（対応する `weeks/*.html` が存在しないため）
- `items` の `why` は、各ニュースの「で、どうしたらええか」を一言に圧縮したもの。**記事の要約にせず、採用の条件で名指しした「読み手の◯◯という作業が、こう変わる」をそのまま圧縮する**
- **掲載0件だった週も必ず書く**（`{"date": "...", "count": 0, "items": []}`）。書かんと「壊れとるのか本当にニュースが無いのか」の区別がつかんくなる

## 7. コミットして push

`main` ブランチに直接 commit & push する。コミット対象に `latest.json` も含めること。コミットメッセージは以下のいずれか：
- 掲載あり：`第N回（YYYY-MM-DD〜YYYY-MM-DD）を公開`
- 0件の週：`YYYY-MM-DD 該当なし（latest.jsonのみ更新）`

push が拒否された場合は、無理に別ブランチへ逃がさず、その旨をはっきり報告して終わる。

## 8. 最後に死活のpingを打つ

**pingを打つかどうかは状況によって変わる。** 打ち漏らしは「壊れた」と誤検知される誤報になるし、打ちすぎると本当に壊れているのを隠す。以下の表に厳密に従うこと。

| 状況 | pingを打つか |
|---|---|
| 通常どおり公開できた | 打つ |
| ニュース0件で `latest.json` だけ更新した | 打つ（正常に判断できた＝仕組みは生きとる） |
| 本日分が既に作成済みで何もせんかった | 打つ |
| push が拒否された | **打たない** |
| 途中でエラーになって最後まで実行できんかった | **打たない** |

打つ場合のコマンド：

```
curl -fsS -m 10 --retry 3 "$HEALTHCHECK_PING_URL" || true
```

★ `$HEALTHCHECK_PING_URL` の実体は、**この手順書には書かれていない**。**routineの起動プロンプトで毎回渡される**ので、そこに書かれたURLを使うこと。見当たらない場合は、pingを打たずにその旨を報告して終わる（勝手にURLを探したり推測したりしない）。このリポジトリは公開されており、URLが漏れると第三者が偽のpingを打てて、壊れているのに監視が鳴らなくなるため、手順書には書かない。

## 9. 報告

最後に次を報告する：
- 掲載した件数と、各記事の見出し
- **落とした候補があれば、その理由**（リンク切れ・日付が範囲外・過去回と重複・読み手やクライアントへの効き先を名指しできず・読み手が持ってへん道具限定の機能・音声/BGM系で実写撮影が未開始のため保留 など）
- push が成功したかどうか

## 異常時の扱い（迷わずここに従う）

判断に迷ったら**何も壊さずに終わる**のが正解。中途半端に書き換えるより、何もせず報告するほうが良い。

### A. 掲載できるニュースが0件だった場合
`index.html` も `weeks/` も**一切変更しない**。`latest.json` だけは `count:0` で更新してコミット・push する（手順6・7参照）。
「対象期間に該当なし。理由（重複／出典が確認できず／採用の条件（手順3）を満たさず＝効き先を名指しできず・持ってへん道具限定・音声系で実写未開始／そもそも該当なし）」を報告して終わる。
**pingは打つ**（latest.json更新まで正常に完了しているため）。

### B. `weeks/<今日の日付>.html` が既に存在する場合
その日はもう週報が作られている。**上書きは絶対にしない。**
連番（`-2` など）でファイルを増やすこともしない。何も変更せず、「本日分は作成済み」と報告して終わる。
**pingは打つ**（無害。二重起動だと分かるだけ）。

### C. 出典を確認できた候補が3件未満だった場合
少なすぎるページを公開するより、出さないほうが良い。A と同じ扱いにする。

### D. push が拒否された場合
`claude/` 等の別ブランチに逃がさない。拒否された事実をそのまま報告して終わる。
**pingは打たない。**

## やってはいけないこと

- 出典を開かずに書く
- 件数合わせのための水増し
- 架空のニュース・架空のURL
- デザインを毎週変える
- 確認していない数字や機能を書く
